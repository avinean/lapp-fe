export enum VisibilityStatus {
    Draft = 'draft',
    Published = 'published',
    Archived = 'archived'
}


export enum BrandType {
  Product = 'product',
  Material = 'material',
}

export enum AppType {
  Quiz = 'quiz',
}